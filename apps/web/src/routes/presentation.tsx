import type { TableResultSchema } from "@locals/bench/bench.schemas";
import { validators } from "@locals/bench/benchmarks";
import ComWorker from "@locals/bench-worker/comlinkWorker?worker";
import { useQuery } from "@tanstack/react-query";
import { createFileRoute, Link } from "@tanstack/react-router";
import { proxy, wrap } from "comlink";
import type { PropsWithChildren, ReactElement } from "react";
import { Fragment, useCallback, useEffect, useState } from "react";
import type { z } from "zod";

type TableResult = z.infer<typeof TableResultSchema>;

// Define slide type to help TypeScript inference
interface SlideData {
  title: string;
  content: ReactElement;
}

export const Route = createFileRoute("/presentation")({
  component: PresentationComponent,
  validateSearch: (search: Record<string, unknown>) => {
    return {
      slide: Number(search?.slide) || 0,
    };
  },
});

const worker = new ComWorker({
  name: "comlink-bench-worker",
});
const workerApi = wrap<import("@locals/bench-worker/comlinkWorker").ComlinkWorker>(worker);

function useBenchmarkData(formState: { time: number; iterations: number; selectedValidators: string[] }) {
  const [progress, setProgress] = useState("");

  return {
    progress,
    ...useQuery({
      queryKey: ["bench", formState.time, formState.iterations, formState.selectedValidators],
      queryFn: () =>
        workerApi
          .benchWorker(formState.time, formState.iterations, proxy(setProgress), formState.selectedValidators)
          .then((v) => {
            return v.filter((result) => formState.selectedValidators.includes(result["Task name"]));
          }),
      staleTime: Infinity,
      placeholderData: (a) => a,
    }),
  };
}

function IntroSlide() {
  return (
    <div className="text-center stack">
      <h2 className="no-margin">The Traditional Approach</h2>
      <div className="stack">
        <p className="no-margin">For years, we've relied on type casting API responses:</p>
        <div className="box" style={{ padding: "1.5rem", textAlign: "left" }}>
          <pre className="no-margin">
            <code>{`const user = await fetch('/api/user/123')
  .then(res => res.json()) as User;
// Or it can look like this
// const user = axios.get<User>("/api/user/123")


// We trust the API response matches our type
console.log(user.name); // What if this fails?
`}</code>
          </pre>
        </div>
        <aside
          className="card"
          style={{
            borderColor: "var(--success)",
            backgroundColor: "var(--success-2)",
          }}
        >
          <p className="font-bold no-margin">But what happens when the API changes or returns unexpected data?</p>
        </aside>
      </div>
    </div>
  );
}

function OpenAPISlide() {
  return (
    <div className="text-center stack">
      <h2 className="no-margin">The OpenAPI Evolution</h2>
      <div className="stack">
        <p className="no-margin">We evolved to use OpenAPI specs with tools like Hey API:</p>
        <div className="box" style={{ padding: "1.5rem", textAlign: "left" }}>
          <pre className="no-margin">
            <code>{`// Generated by @hey-api/openapi-ts
import { addUser } from './client/sdk.gen';
import type { AddUserData, User } from './client/types.gen';

// Type-safe at compile time
const newUser = await addUser({
  body: {
    name: 'John Doe',
    email: 'john@example.com'
  }
});

// TypeScript knows this is User type
console.log(newUser.data.name);`}</code>
          </pre>
        </div>
        <aside
          className="card"
          style={{
            borderColor: "var(--yellow)",
            backgroundColor: "var(--yellow-2)",
          }}
        >
          <h3 className="no-margin" style={{ color: "inherit" }}>
            But what runtime protections do TypeScript types provide?
          </h3>
          <p className="no-margin" style={{ marginTop: "0.5rem" }}>
            TypeScript types are erased at runtime. Your application can still crash from unexpected data.
          </p>
        </aside>
      </div>
    </div>
  );
}

function PerformanceMattersSlide() {
  return (
    <div className="stack">
      <div className="text-center">
        <h2 className="no-margin">Why Runtime Validation Matters</h2>
        <p className="text-muted no-margin">When types aren't enough, validation becomes your safety net</p>
      </div>

      <div className="layout-split">
        <div className="stack">
          <aside
            className="card"
            style={{
              borderColor: "var(--error)",
              backgroundColor: "var(--error-2)",
            }}
          >
            <h3 className="no-margin" style={{ color: "inherit" }}>
              🚨 The Problem
            </h3>
            <ul className="no-list stack" style={{ marginTop: "0.5rem" }}>
              <li>• External APIs change without notice</li>
              <li>• User input can be malicious or malformed</li>
              <li>• TypeScript types are erased at runtime</li>
              <li>• Network requests can return unexpected data</li>
              <li>• Third-party integrations may fail silently</li>
            </ul>
          </aside>

          <aside
            className="card"
            style={{
              borderColor: "var(--primary)",
              backgroundColor: "var(--primary-2)",
            }}
          >
            <h3 className="no-margin" style={{ color: "inherit" }}>
              🛡️ The Solution
            </h3>
            <ul className="no-list stack" style={{ marginTop: "0.5rem" }}>
              <li>• Validate data at runtime boundaries</li>
              <li>• Fail fast with clear error messages</li>
              <li>• Transform and sanitize untrusted input</li>
              <li>• Ensure type safety beyond compile time</li>
              <li>• Build robust, production-ready applications</li>
            </ul>
          </aside>
        </div>

        <div className="stack">
          <aside
            className="card"
            style={{
              borderColor: "var(--success)",
              backgroundColor: "var(--success-2)",
            }}
          >
            <h3 className="no-margin" style={{ color: "inherit" }}>
              🎯 Critical Use Cases
            </h3>
            <div className="stack" style={{ marginTop: "1rem" }}>
              <div className="cluster">
                <BulletNumber>1</BulletNumber>
                <div className="stack">
                  <div className="font-bold">API Boundaries</div>
                  <div className="fs-xs">Validate external API responses and incoming requests</div>
                </div>
              </div>

              <div className="cluster">
                <BulletNumber>2</BulletNumber>
                <div className="stack">
                  <div className="font-bold">User Input</div>
                  <div className="fs-xs">Forms, uploads, and any user-generated content</div>
                </div>
              </div>

              <div className="cluster">
                <BulletNumber>3</BulletNumber>
                <div className="stack">
                  <div className="font-bold">Data Streams</div>
                  <div className="fs-xs">Real-time events, webhooks, and message queues</div>
                </div>
              </div>

              <div className="cluster">
                <BulletNumber>4</BulletNumber>
                <div className="stack">
                  <div className="font-bold">Configuration</div>
                  <div className="fs-xs">Environment variables and config files</div>
                </div>
              </div>
            </div>
          </aside>
        </div>
      </div>

      <div
        className="box"
        style={{ padding: "1.5rem", background: "linear-gradient(135deg, var(--purple-1), var(--pink-1))" }}
      >
        <div className="text-center stack">
          <h3 className="no-margin" style={{ color: "var(--purple)" }}>
            ⚡ Performance is Critical
          </h3>
          <p className="no-margin">
            Validation happens on every request, every form submission, every data transformation.
            <br />
            <strong>Choosing the right validator can make or break your application's performance.</strong>
          </p>
          <div className="layout-three-col" style={{ marginTop: "1rem" }}>
            <div className="box text-center">
              <div className="no-margin" style={{ color: "var(--purple)" }}>
                10,000+
              </div>
              <div className="fs-xs text-muted">Validations per second</div>
            </div>
            <div className="box text-center">
              <div className="no-margin" style={{ color: "var(--purple)" }}>
                100x
              </div>
              <div className="fs-xs text-muted">Performance difference</div>
            </div>
            <div className="box text-center">
              <div className="no-margin" style={{ color: "var(--purple)" }}>
                DX
              </div>
              <div className="fs-xs text-muted">Developer experience matters</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}

function PropPollutionSlide() {
  return (
    <div className="stack">
      <div className="text-center">
        <h2 className="no-margin">Unforeseen issues: Property Pollution</h2>
        <p className="text-muted no-margin">External data often contains more than you expect</p>
      </div>

      <div className="layout-split">
        <div className="stack">
          <div
            className="card"
            style={{
              borderColor: "var(--error)",
              backgroundColor: "var(--error-2)",
            }}
          >
            <header>
              <h3 className="no-margin">⚠️ Raw API Response</h3>
            </header>
            <pre className="no-margin" style={{ padding: "1rem" }}>
              <code>{`// What the API actually returns
{
  "id": 123,
  "name": "John Doe",
  "email": "john@example.com",
  // Unexpected extras:
  "internal_notes": "VIP customer",
  "password_hash": "$2b$10$...",
  "admin_flags": ["delete_users"],
  "debug_info": { "session": "abc123" },
  "experimental_features": true
}`}</code>
            </pre>
            <footer className="fs-xs text-muted" style={{ padding: "0.5rem 1rem" }}>
              This data can leak sensitive information or cause security vulnerabilities.
            </footer>
          </div>

          <aside
            className="card"
            style={{
              borderColor: "var(--warning)",
              backgroundColor: "var(--warning-2)",
            }}
          >
            <h3 className="no-margin" style={{ color: "inherit" }}>
              😬 Type Casting Problem
            </h3>
            <pre
              className="no-margin"
              style={{ padding: "1rem", color: "inherit", background: "rgba(0,0,0,0.15)", borderRadius: "var(--br-m)" }}
            >
              <code>{`interface User {
  id: number;
  name: string;
  email: string;
}

const user = apiResponse as User;
// All extra properties are still there!
console.log(user);
// Shows: { id: 123, name: "John", email: "john@...",
//          password_hash: "$2b$10$...", ... }`}</code>
            </pre>
          </aside>
        </div>

        <div className="stack">
          <aside
            className="card"
            style={{
              borderColor: "var(--success)",
              backgroundColor: "var(--success-2)",
            }}
          >
            <h3 className="no-margin" style={{ color: "inherit" }}>
              ✅ Zod Strips Excess Properties
            </h3>
            <pre
              className="no-margin"
              style={{ padding: "1rem", color: "inherit", background: "rgba(0,0,0,0.15)", borderRadius: "var(--br-m)" }}
            >
              <code>{`import { z } from 'zod';

const UserSchema = z.object({
  id: z.number(),
  name: z.string(),
  email: z.string().email()
});

const user = UserSchema.parse(apiResponse);
console.log(user);
// Shows only: { id: 123, name: "John Doe",
//               email: "john@example.com" }

// Sensitive data automatically stripped!`}</code>
            </pre>
            <p className="fs-xs" style={{ marginTop: "0.5rem" }}>
              By default, Zod removes unknown properties, protecting your application.
            </p>
          </aside>

          <aside
            className="card"
            style={{
              borderColor: "var(--primary)",
              backgroundColor: "var(--primary-2)",
            }}
          >
            <h3 className="no-margin" style={{ color: "inherit" }}>
              🔒 Security Benefits
            </h3>
            <ul className="no-list stack" style={{ marginTop: "0.5rem" }}>
              <li>• Prevents sensitive data leaks to frontend</li>
              <li>• Reduces attack surface area</li>
              <li>• Ensures data consistency across your app</li>
              <li>• Catches unexpected API changes early</li>
              <li>• Makes debugging easier with known data shape</li>
            </ul>
          </aside>
        </div>
      </div>
    </div>
  );
}

function HeyAPIPluginsSlide() {
  return (
    <div className="stack">
      <div className="text-center">
        <h2 className="no-margin">Hey API: Pluggable Validation</h2>
        <p className="text-muted no-margin">Choose your validation library with OpenAPI code generation</p>
      </div>

      <div className="layout-split">
        <div className="stack">
          <aside
            className="card"
            style={{
              borderColor: "var(--primary)",
              backgroundColor: "var(--primary-2)",
            }}
          >
            <h3 className="no-margin" style={{ color: "inherit" }}>
              🧩 Plugin Architecture
            </h3>
            <p className="no-margin" style={{ marginTop: "0.5rem" }}>
              Hey API supports validation plugins for popular libraries, giving you runtime safety with your preferred
              tool.
            </p>
            <div className="stack" style={{ marginTop: "1rem" }}>
              <div className="cluster">
                <span className="tag success">Zod</span>
                <span>TypeScript-first schema validation</span>
              </div>
              <div className="cluster">
                <span className="tag info">Valibot</span>
                <span>Modular, tree-shakeable</span>
              </div>
              <div className="cluster">
                <span className="tag warning">ArkType</span>
                <span>TypeScript-native validation</span>
              </div>
              <div className="cluster">
                <span className="tag error">Effect Schema</span>
                <span>Functional approach</span>
              </div>
            </div>
          </aside>

          <div
            className="card"
            style={{
              borderColor: "var(--green)",
              backgroundColor: "var(--green-2)",
            }}
          >
            <header>
              <h3 className="no-margin">🎯 Configuration Example</h3>
            </header>
            <pre className="no-margin" style={{ padding: "1rem" }}>
              <code>{`// openapi-ts.config.ts
export default {
  input: 'openapi.json',
  output: './src/client',
  plugins: [
    '@hey-api/typescript',
    '@hey-api/sdk',
    {
      name: '@hey-api/schemas',
      type: 'zod', // or 'valibot', 'arktype'
    },
  ],
};`}</code>
            </pre>
          </div>
        </div>

        <div className="stack">
          <div
            className="card"
            style={{
              borderColor: "var(--purple)",
              backgroundColor: "var(--purple-2)",
            }}
          >
            <header>
              <h3 className="no-margin">⚡ Generated Output</h3>
            </header>
            <pre className="no-margin" style={{ padding: "1rem" }}>
              <code>{`// Generated schemas.gen.ts
export const UserSchema = z.object({
  id: z.number(),
  name: z.string(),
  email: z.string().email(),
});

// Generated SDK with validation
export async function getUser(id: number) {
  const response = await fetch(\`/api/users/\${id}\`);
  const data = await response.json();

  // Automatic runtime validation!
  return UserSchema.parse(data);
}`}</code>
            </pre>
          </div>

          <aside
            className="card"
            style={{
              borderColor: "var(--yellow)",
              backgroundColor: "var(--yellow-2)",
            }}
          >
            <h3 className="no-margin" style={{ color: "inherit" }}>
              ✨ Benefits
            </h3>
            <ul className="no-list stack" style={{ marginTop: "0.5rem" }}>
              <li>
                • Type-safe at compile time <strong>AND</strong> runtime
              </li>
              <li>• Consistent validation across your application</li>
              <li>• Generated from your OpenAPI spec</li>
              <li>• Choose the validator that fits your needs</li>
              <li>• Automatic schema updates with API changes</li>
            </ul>
          </aside>
        </div>
      </div>
    </div>
  );
}

function StandardSchemaSlide() {
  return (
    <div className="stack">
      <div className="text-center">
        <h2 className="no-margin">Standard Schema: Unifying Validation</h2>
        <p className="text-muted no-margin">A common interface for TypeScript validation libraries</p>
      </div>

      <div className="layout-split">
        <div className="stack">
          <aside
            className="card"
            style={{
              borderColor: "var(--primary)",
              backgroundColor: "var(--primary-2)",
            }}
          >
            <h3 className="no-margin" style={{ color: "inherit" }}>
              🎯 The Problem
            </h3>
            <p className="no-margin" style={{ marginTop: "0.5rem" }}>
              Every validation library has its own API. Tools must write custom adapters for each one.
            </p>
            <pre
              className="no-margin"
              style={{
                padding: "1rem",
                color: "inherit",
                background: "rgba(0,0,0,0.15)",
                borderRadius: "var(--br-m)",
                marginTop: "1rem",
              }}
            >
              <code>{`// Different validation APIs
const zodResult = zodSchema.parse(data);
const valibotResult = v.parse(valibotSchema, data);
const arktypeResult = arktypeSchema(data);

// Tools need adapters for each!`}</code>
            </pre>
          </aside>

          <aside
            className="card"
            style={{
              borderColor: "var(--green)",
              backgroundColor: "var(--green-2)",
            }}
          >
            <h3 className="no-margin" style={{ color: "inherit" }}>
              ⚡ The Solution
            </h3>
            <p className="no-margin" style={{ marginTop: "0.5rem" }}>
              Standard Schema provides one interface that all validation libraries can implement.
            </p>
            <pre
              className="no-margin"
              style={{
                padding: "1rem",
                color: "inherit",
                background: "rgba(0,0,0,0.15)",
                borderRadius: "var(--br-m)",
                marginTop: "1rem",
              }}
            >
              <code>{`// Unified validation interface
function validate<T extends StandardSchemaV1>(
  schema: T,
  data: unknown
) {
  const result = schema['~standard'].validate(data);
  if (result.issues) throw new Error('Invalid');
  return result.value;
}`}</code>
            </pre>
          </aside>
        </div>

        <div className="stack">
          <aside
            className="card"
            style={{
              borderColor: "var(--primary)",
              backgroundColor: "var(--primary-2)",
            }}
          >
            <h3 className="no-margin" style={{ color: "inherit" }}>
              🏗️ Who's Behind It
            </h3>
            <p className="no-margin" style={{ marginTop: "0.5rem" }}>
              Created by the makers of <strong>Zod</strong>, <strong>Valibot</strong>, and <strong>ArkType</strong>.
            </p>
            <ul className="no-list stack" style={{ marginTop: "1rem" }}>
              <li>
                <span className="tag success" style={{ marginRight: "0.5rem" }}>
                  Active
                </span>
                <span>Already implemented by 20+ validation libraries</span>
              </li>
              <li>
                <span className="tag info" style={{ marginRight: "0.5rem" }}>
                  Adopted
                </span>
                <span>Adopted by 30+ tools and frameworks</span>
              </li>
              <li>
                <span className="tag warning" style={{ marginRight: "0.5rem" }}>
                  TRPC
                </span>
                <span>Including tRPC, TanStack, Hono, and more</span>
              </li>
            </ul>
          </aside>

          <aside
            className="card"
            style={{
              borderColor: "var(--warning)",
              backgroundColor: "var(--warning-2)",
            }}
          >
            <h3 className="no-margin" style={{ color: "inherit" }}>
              🚀 Benefits
            </h3>
            <ul className="no-list stack" style={{ marginTop: "0.5rem" }}>
              <li>
                • <strong>Ecosystem unification:</strong> Write once, support all validators
              </li>
              <li>
                • <strong>Developer choice:</strong> Pick your preferred validation library
              </li>
              <li>
                • <strong>Future-proof:</strong> New validators work automatically
              </li>
              <li>
                • <strong>Type safety:</strong> Full TypeScript support
              </li>
              <li>
                • <strong>Zero dependencies:</strong> Just a TypeScript interface
              </li>
            </ul>
          </aside>

          <div className="box text-center" style={{ padding: "1rem" }}>
            <p className="no-margin fs-xs">
              🌐 Learn more at{" "}
              <a href="https://standardschema.dev" target="_blank" rel="noopener noreferrer">
                standardschema.dev
              </a>
            </p>
          </div>
        </div>
      </div>
    </div>
  );
}

function ThroughputSlide({
  data,
  onStartBenchmark,
  time,
  setTime,
  iterations,
  setIterations,
  selectedValidators,
  setSelectedValidators,
  progress,
  status,
  isPlaceholderData,
}: {
  data: TableResult[];
  onStartBenchmark: () => void;
  time: number;
  setTime: React.Dispatch<React.SetStateAction<number>>;
  iterations: number;
  setIterations: React.Dispatch<React.SetStateAction<number>>;
  selectedValidators: string[];
  setSelectedValidators: React.Dispatch<React.SetStateAction<string[]>>;
  progress: string;
  status: string;
  isPlaceholderData: boolean;
}) {
  const throughputData = data
    .map((item) => ({
      name: item["Task name"],
      throughput: parseInt(item["Throughput avg (ops/s)"].split(" ±")[0]!.replace(/,/g, ""), 10),
      latency: parseFloat(item["Latency avg (ns)"].split(" ±")[0]!),
    }))
    .sort((a, b) => b.throughput - a.throughput);

  const maxThroughput = Math.max(...throughputData.map((d) => d.throughput));

  return (
    <div className="stack">
      <div className="box">
        <h3 className="no-margin" style={{ marginBottom: "1rem" }}>
          Benchmark Configuration
        </h3>

        {/* Progress section similar to index.tsx */}
        <div style={{ marginBottom: "1rem" }}>
          <div className="text-center">
            {isPlaceholderData || (status !== "success" && status !== "error") ? (
              <span>Benchmark is loading...</span>
            ) : status === "success" ? (
              <span className="tag success">Success!</span>
            ) : status === "error" ? (
              <span className="tag error">Error</span>
            ) : null}
          </div>
          {(isPlaceholderData || status === "pending") && (
            <div className="text-center">
              {progress && (
                <span className="text-muted">
                  Currently running:
                  <span className="tag success" style={{ marginLeft: "0.5rem" }}>
                    {progress}
                  </span>
                </span>
              )}
            </div>
          )}
        </div>
        <div className="layout-three-col">
          <div>
            <label htmlFor="time" className="font-medium" style={{ display: "block", marginBottom: "0.25rem" }}>
              Time (ms)
            </label>

            {/* biome-ignore lint/correctness/useUniqueElementIds: Ignoring... */}
            <input
              type="number"
              id="time"
              min="1"
              value={time}
              onChange={(e) => setTime(Number(e.target.value))}
              disabled={isPlaceholderData || status === "pending"}
            />
          </div>
          <div>
            <label htmlFor="iterations" className="font-medium" style={{ display: "block", marginBottom: "0.25rem" }}>
              Iterations
            </label>
            {/* biome-ignore lint/correctness/useUniqueElementIds: Ignoring... */}
            <input
              id="iterations"
              type="number"
              min="1"
              value={iterations}
              onChange={(e) => setIterations(Number(e.target.value))}
              disabled={isPlaceholderData || status === "pending"}
            />
          </div>
          <div>
            <label htmlFor="runBench" className="font-medium" style={{ display: "block", marginBottom: "0.25rem" }}>
              Run Benchmark
            </label>
            {/* biome-ignore lint/correctness/useUniqueElementIds: Ignoring... */}
            <button
              id="runBench"
              onClick={onStartBenchmark}
              disabled={isPlaceholderData || status === "pending"}
              className="primary full"
              type="button"
            >
              {isPlaceholderData || status === "pending" ? "Running..." : "Start Benchmark"}
            </button>
          </div>
        </div>

        <div style={{ marginTop: "1rem" }}>
          <span className="font-medium" style={{ display: "block", marginBottom: "0.5rem" }}>
            Selected Validators ({selectedValidators.length})
          </span>
          <div className="grid auto" style={{ "--grid-min": "140px" }}>
            {validators.map((v) => (
              <label key={v.name} className="form-option-row">
                <input
                  type="checkbox"
                  checked={selectedValidators.includes(v.name)}
                  onChange={(e) => {
                    if (e.target.checked) {
                      setSelectedValidators((prev) => [...prev, v.name]);
                    } else {
                      setSelectedValidators((prev) => prev.filter((name) => name !== v.name));
                    }
                  }}
                  disabled={isPlaceholderData || status === "pending"}
                />
                <span>{v.name}</span>
              </label>
            ))}
          </div>
        </div>
      </div>

      <div className="stack">
        <div className="split center">
          <div className="text-muted">
            {isPlaceholderData || status === "pending" ? "Running benchmarks..." : `${data.length} validators tested`}
          </div>
          <div className="text-muted">
            {throughputData.length > 0 && `${(throughputData[0]!.throughput / 1000000).toFixed(1)}M ops/s (best)`}
          </div>
        </div>

        {throughputData.length > 0 ? (
          <div
            className="grid"
            style={{
              gridTemplateColumns: "auto 1fr",
            }}
          >
            {throughputData.map((item, index) => (
              <Fragment key={item.name}>
                <div className="font-bold">{item.name}</div>
                <div
                  className="tag"
                  style={{
                    width: `${(item.throughput / maxThroughput) * 100}%`,
                    backgroundColor: index === 0 ? "var(--green-5)" : index === 1 ? "var(--blue-5)" : "var(--gray-3)",
                    display: "block",
                    textAlign: "right",
                    paddingRight: "0.5rem",
                  }}
                >
                  {(item.throughput / 1000000).toFixed(1)}M ops/s
                </div>
              </Fragment>
            ))}
          </div>
        ) : (
          <div className="text-center text-muted" style={{ padding: "2rem" }}>
            {isPlaceholderData || status === "pending"
              ? "Running benchmarks..."
              : "Click 'Start Benchmark' to see live results"}
          </div>
        )}

        {throughputData.length > 0 && (
          <div className="text-muted" style={{ marginTop: "1rem" }}>
            Higher is better. {throughputData[0]?.name} leads with{" "}
            {(throughputData[0]!.throughput / 1000000).toFixed(1)}M operations per second.
          </div>
        )}
      </div>
    </div>
  );
}

function InsightsSlide({
  data,
  formState,
}: {
  data: TableResult[];
  formState: {
    time: number;
    iterations: number;
    selectedValidators: string[];
  };
}) {
  const throughputData = data
    .map((item) => ({
      name: item["Task name"],
      throughput: parseInt(item["Throughput avg (ops/s)"].split(" ±")[0]!.replace(/,/g, ""), 10),
      latency: parseFloat(item["Latency avg (ns)"].split(" ±")[0]!),
    }))
    .sort((a, b) => b.throughput - a.throughput);

  const fastest = throughputData[0];
  const slowest = throughputData[throughputData.length - 1];
  const performanceSpread = fastest && slowest ? (fastest.throughput / slowest.throughput).toFixed(1) : "N/A";

  return (
    <div className="stack">
      {/* Benchmark Configuration Summary */}
      <aside
        className="card"
        style={{
          borderColor: "var(--primary)",
          backgroundColor: "var(--primary-2)",
        }}
      >
        <h3 className="no-margin" style={{ color: "inherit" }}>
          📋 Benchmark Configuration
        </h3>
        <div className="layout-three-col" style={{ marginTop: "1rem" }}>
          <div>
            <span className="font-medium">Duration:</span> {formState.time}ms
          </div>
          <div>
            <span className="font-medium">Iterations:</span> {formState.iterations}
          </div>
          <div>
            <span className="font-medium">Validators tested:</span> {formState.selectedValidators.length} of{" "}
            {validators.length}
          </div>
        </div>
        {formState.selectedValidators.length < validators.length && (
          <div className="fs-xs" style={{ marginTop: "0.5rem" }}>
            <span className="font-medium">Selected:</span> {formState.selectedValidators.join(", ")}
          </div>
        )}
      </aside>

      {throughputData.length > 0 ? (
        <>
          <div className="layout-split">
            <aside
              className="card"
              style={{
                borderColor: "var(--success)",
                backgroundColor: "var(--success-2)",
              }}
            >
              <h3 className="no-margin" style={{ color: "inherit" }}>
                🏆 Top Performers
              </h3>
              <ul className="no-list stack" style={{ marginTop: "0.5rem" }}>
                {throughputData.slice(0, 3).map((item) => (
                  <li key={item.name}>
                    <strong>{item.name}:</strong> {(item.throughput / 1000000).toFixed(1)}M ops/s
                  </li>
                ))}
              </ul>
            </aside>
            <aside
              className="card"
              style={{
                borderColor: "var(--warning)",
                backgroundColor: "var(--warning-2)",
              }}
            >
              <h3 className="no-margin" style={{ color: "inherit" }}>
                ⚡ Low Latency Leaders
              </h3>
              <ul className="no-list stack" style={{ marginTop: "0.5rem" }}>
                {throughputData
                  .sort((a, b) => a.latency - b.latency)
                  .slice(0, 3)
                  .map((item) => (
                    <li key={item.name}>
                      <strong>{item.name}:</strong> {item.latency.toFixed(0)}ns avg
                    </li>
                  ))}
              </ul>
            </aside>
          </div>
          <aside
            className="card"
            style={{
              borderColor: "var(--error)",
              backgroundColor: "var(--error-2)",
            }}
          >
            <h3 className="no-margin" style={{ color: "inherit" }}>
              📊 Performance Spread
            </h3>
            <p className="no-margin" style={{ marginTop: "0.5rem" }}>
              Performance varies by <strong>{performanceSpread}x</strong> between fastest ({fastest?.name}) and slowest
              ({slowest?.name}). Choose wisely based on your performance requirements.
            </p>
          </aside>
        </>
      ) : (
        <div className="text-center text-muted" style={{ padding: "2rem" }}>
          Run benchmarks to see performance insights
        </div>
      )}
    </div>
  );
}

function ArkTypeEnvSlide() {
  return (
    <div className="stack">
      <div className="text-center">
        <h2 className="no-margin">ArkType: Environment Variable Validation</h2>
        <p className="text-muted no-margin">Type-safe environment parsing with runtime validation</p>
      </div>

      <aside
        className="card"
        style={{
          borderColor: "var(--gray)",
          backgroundColor: "var(--gray-2)",
        }}
      >
        <h3 className="no-margin" style={{ color: "inherit" }}>
          💡 Why This Matters
        </h3>
        <p className="no-margin" style={{ marginTop: "0.5rem" }}>
          <strong>Fail Fast:</strong> Catch configuration errors at startup, not in production.
        </p>
      </aside>

      <div className="layout-split">
        <div className="stack">
          <div className="card">
            <header>
              <h3 className="no-margin">❌ The Old Way</h3>
            </header>
            <pre className="no-margin" style={{ padding: "1rem" }}>
              <code>{`// Unsafe environment access
const config = {
  port: Number(process.env.PORT) || 3000,
  database: process.env.DATABASE_URL || '',
  apiKey: process.env.API_KEY || '',
  debug: process.env.NODE_ENV === 'development'
};

// What happens when:
// - PORT is "abc"?
// - DATABASE_URL is missing?
// - API_KEY is empty?`}</code>
            </pre>
            <footer className="fs-xs text-muted" style={{ padding: "0.5rem 1rem" }}>
              Runtime errors, silent failures, and security vulnerabilities.
            </footer>
          </div>

          <aside
            className="card"
            style={{
              borderColor: "var(--warning)",
              backgroundColor: "var(--warning-2)",
            }}
          >
            <h3 className="no-margin" style={{ color: "inherit" }}>
              ⚠️ Common Issues
            </h3>
            <ul className="no-list stack" style={{ marginTop: "0.5rem" }}>
              <li>• Invalid port numbers causing server crashes</li>
              <li>• Missing database URLs in production</li>
              <li>• Empty API keys leading to authentication failures</li>
              <li>• Type coercion bugs (strings vs numbers)</li>
              <li>• No validation at startup time</li>
            </ul>
          </aside>
        </div>

        <div className="stack">
          <aside
            className="card"
            style={{
              borderColor: "var(--success)",
              backgroundColor: "var(--success-2)",
            }}
          >
            <h3 className="no-margin" style={{ color: "inherit" }}>
              ✅ ArkType Solution
            </h3>
            <pre
              className="no-margin"
              style={{ padding: "1rem", color: "inherit", background: "rgba(0,0,0,0.15)", borderRadius: "var(--br-m)" }}
            >
              <code>{`import { type } from 'arktype';

const envSchema = type({
  DATABASE_HOST: "string.host",
  DATABASE_PORT: "number.port",
  API_KEY: "string>10",
  "NODE_ENV?": "'development' | 'production' | 'test'",
  "DEBUG?": "string.alpha.toLowerCase.pipe(v => v === 'true')"
});

// Parse and validate at startup
const config = envSchema(process.env);

if (config instanceof type.errors) {
  console.error("Invalid environment:", config.summary);
  process.exit(1);
}

// Now 'config' is fully typed and validated!
console.log(\`Server starting on port \${config.DATABASE_PORT}\`);`}</code>
            </pre>
          </aside>
        </div>
      </div>
    </div>
  );
}

function ServiceWorkersSlide() {
  return (
    <div className="stack">
      <div className="text-center">
        <h2 className="no-margin">Built with Web Workers</h2>
      </div>
      <div className="layout-split">
        <aside
          className="card"
          style={{
            borderColor: "var(--primary)",
            backgroundColor: "var(--primary-2)",
          }}
        >
          <h3 className="no-margin" style={{ color: "inherit" }}>
            What are Web Workers?
          </h3>
          <p className="no-margin" style={{ marginTop: "0.5rem" }}>
            Web Workers allow JavaScript to run computationally intensive tasks in background threads, separate from the
            main UI thread. This prevents blocking user interactions during heavy operations.
          </p>
          <p className="no-margin" style={{ marginTop: "0.5rem" }}>
            This benchmark app uses Web Workers to keep the interface smooth and responsive while running validation
            benchmarks that would otherwise freeze the UI.
          </p>
        </aside>

        <aside
          className="card"
          style={{
            borderColor: "var(--green)",
            backgroundColor: "var(--green-2)",
          }}
        >
          <h3 className="no-margin" style={{ color: "inherit" }}>
            Implementation with Comlink
          </h3>
          <p className="no-margin" style={{ marginTop: "0.5rem" }}>
            We use{" "}
            <a href="https://github.com/GoogleChromeLabs/comlink" target="_blank" rel="noopener noreferrer">
              Comlink
            </a>{" "}
            by Google Chrome Labs to simplify worker communication.
          </p>
          <p className="no-margin" style={{ marginTop: "0.5rem" }}>
            Comlink makes Web Workers feel like regular async functions, handling all the message passing complexity for
            us.
          </p>
        </aside>
      </div>
    </div>
  );
}

function ChoosingValidatorSlide() {
  return (
    <div className="stack">
      <div className="layout-three-col">
        <aside
          className="card"
          style={{
            borderColor: "var(--success)",
            backgroundColor: "var(--success-2)",
          }}
        >
          <h3 className="no-margin" style={{ color: "inherit" }}>
            🚀 High Performance
          </h3>
          <div className="stack" style={{ marginTop: "1rem" }}>
            <div>
              <strong>io-ts:</strong> Functional approach, excellent for functional programming codebases
            </div>
            <div>
              <strong>ArkType:</strong> TypeScript-first, compile-time optimizations
            </div>
            <div>
              <strong>ajv:</strong> JSON Schema standard, mature ecosystem
            </div>
          </div>
        </aside>
        <aside
          className="card"
          style={{
            borderColor: "var(--primary)",
            backgroundColor: "var(--primary-2)",
          }}
        >
          <h3 className="no-margin" style={{ color: "inherit" }}>
            ⚖️ Balanced Choice
          </h3>
          <div className="stack" style={{ marginTop: "1rem" }}>
            <div>
              <strong>zod:</strong> Great DX, TypeScript integration, good performance
            </div>
            <div>
              <strong>valibot:</strong> Modular, tree-shakeable, growing ecosystem
            </div>
          </div>
        </aside>
        <aside
          className="card"
          style={{
            borderColor: "var(--purple)",
            backgroundColor: "var(--purple-2)",
          }}
        >
          <h3 className="no-margin" style={{ color: "inherit" }}>
            🔧 Feature Rich
          </h3>
          <div className="stack" style={{ marginTop: "1rem" }}>
            <div>
              <strong>yup:</strong> Object schema validation, transforms, async validation
            </div>
            <div>
              <strong>joi:</strong> Rich validation rules, server-side focused
            </div>
          </div>
        </aside>
      </div>
      <div className="text-center" style={{ marginTop: "1rem" }}>
        <div className="box" style={{ padding: "1.5rem" }}>
          <p className="font-bold no-margin">
            Consider: Performance requirements, bundle size, ecosystem, and developer experience
          </p>
          <p className="fs-xs text-muted no-margin" style={{ marginTop: "0.5rem" }}>
            For additional insight into popularity and adoption trends, view{" "}
            <a
              href="https://tanstack.com/stats/npm?packageGroups=%5B%7B%22packages%22%3A%5B%7B%22name%22%3A%22zod%22%7D%5D%2C%22color%22%3A%22%23ef4444%22%7D%2C%7B%22packages%22%3A%5B%7B%22name%22%3A%22io-ts%22%7D%5D%2C%22color%22%3A%22%233b82f6%22%7D%2C%7B%22packages%22%3A%5B%7B%22name%22%3A%22arktype%22%7D%5D%2C%22color%22%3A%22%2310b981%22%7D%2C%7B%22packages%22%3A%5B%7B%22name%22%3A%22valibot%22%7D%5D%2C%22color%22%3A%22%23f97316%22%7D%2C%7B%22packages%22%3A%5B%7B%22name%22%3A%22yup%22%7D%5D%2C%22color%22%3A%22%2306b6d4%22%7D%2C%7B%22packages%22%3A%5B%7B%22name%22%3A%22%40sinclair%2Ftypebox%22%7D%5D%2C%22color%22%3A%22%23d946ef%22%7D%5D&range=365-days&transform=none&binType=weekly&showDataMode=all&height=400"
              target="_blank"
              rel="noopener noreferrer"
            >
              this chart comparing install rates
            </a>{" "}
            of the different packages.
          </p>
        </div>
      </div>
    </div>
  );
}

function PresentationIntroSlide() {
  return (
    <div className="text-center stack">
      <h2 className="no-margin">Type safety vs Runtime safety and why it matters</h2>

      <div className="stack">
        <div
          className="box"
          style={{ padding: "2rem", background: "linear-gradient(135deg, var(--blue-1), var(--purple-1))" }}
        >
          <h3 className="no-margin" style={{ color: "var(--blue)" }}>
            What We'll Cover Today
          </h3>
          <div className="layout-split" style={{ marginTop: "1.5rem" }}>
            <div className="stack text-left">
              <div className="cluster">
                <BulletNumber>1</BulletNumber>
                <div className="stack">
                  <div className="font-bold">Why Runtime Validation Matters</div>
                  <div className="fs-xs text-muted">The gap between TypeScript types and runtime safety</div>
                </div>
              </div>

              <div className="cluster">
                <BulletNumber>2</BulletNumber>
                <div className="stack">
                  <div className="font-bold">Validation library showdown</div>
                  <div className="fs-xs text-muted">Live benchmarks comparing popular validation libraries</div>
                </div>
              </div>
            </div>

            <div className="stack text-left">
              <div className="cluster">
                <BulletNumber>3</BulletNumber>
                <div className="stack">
                  <div className="font-bold">Security Implications</div>
                  <div className="fs-xs text-muted">Property pollution and data sanitization</div>
                </div>
              </div>

              <div className="cluster">
                <BulletNumber>4</BulletNumber>
                <div className="stack">
                  <div className="font-bold">Choosing the Right Tool</div>
                  <div className="fs-xs text-muted">Performance vs features vs developer experience</div>
                </div>
              </div>
            </div>
          </div>
        </div>

        <aside
          className="card"
          style={{
            borderColor: "var(--success)",
            backgroundColor: "var(--success-2)",
          }}
        >
          <h3 className="no-margin" style={{ color: "inherit" }}>
            🎯 Goals
          </h3>
          <p className="no-margin" style={{ marginTop: "0.5rem" }}>
            Raise awareness of different validation libraries and show when and why to use runtime validation. Compare
            performance characteristics and identify which one fits your specific use case.
          </p>
        </aside>
      </div>
    </div>
  );
}

function SpeakerIntroSlide() {
  return (
    <div className="text-center stack">
      <h2 className="no-margin">Who am I?</h2>

      <div className="layout-sidebar">
        <div style={{ justifySelf: "center" }}>
          <span className="avatar xl bordered" style={{ "--avatar-size": "192px" }}>
            <img src="https://avatars.githubusercontent.com/u/34942041?v=4" alt="Sam Peterson" />
          </span>
        </div>

        <div className="stack text-left">
          <div>
            <h3 className="no-margin">Sam Peterson</h3>
            <p className="text-muted no-margin">Senior Full Stack Developer</p>
          </div>

          <div className="stack">
            <aside
              className="card"
              style={{
                borderColor: "var(--amber)",
                backgroundColor: "var(--amber-2)",
              }}
            >
              <h4 className="font-bold no-margin" style={{ color: "inherit" }}>
                💼 Background
              </h4>
              <p className="no-margin" style={{ marginTop: "0.25rem" }}>
                Full stack development since I started in 2018.
              </p>
            </aside>

            <aside
              className="card"
              style={{
                borderColor: "var(--brown)",
                backgroundColor: "var(--brown-2)",
              }}
            >
              <h4 className="font-bold no-margin" style={{ color: "inherit" }}>
                🚀 Interests
              </h4>
              <p className="no-margin" style={{ marginTop: "0.25rem" }}>
                All things web, TypeScript, hacking at side projects, wood working, video games, and enjoying life with
                my wife and dog.
              </p>
            </aside>

            <aside
              className="card"
              style={{
                borderColor: "var(--purple)",
                backgroundColor: "var(--purple-2)",
              }}
            >
              <h4 className="font-bold no-margin" style={{ color: "inherit" }}>
                🔗 Connect
              </h4>
              <div className="split" style={{ marginTop: "0.25rem" }}>
                <span>GitHub: @s-petey</span>
                <span>Bluesky: @sam-peterson</span>
              </div>
            </aside>
          </div>
        </div>
      </div>
    </div>
  );
}

function ThankYouSlide() {
  return (
    <div className="text-center stack">
      <h2 className="h1 no-margin">Thank You!</h2>

      <div className="stack">
        <div
          className="box"
          style={{ padding: "2rem", background: "linear-gradient(135deg, var(--blue-1), var(--purple-1))" }}
        >
          <h3 className="no-margin" style={{ color: "var(--blue)" }}>
            🚀 Explore the Code
          </h3>
          <p className="no-margin" style={{ marginTop: "0.5rem" }}>
            This presentation and benchmark tool are available for the public!
          </p>
          <div style={{ marginTop: "1.5rem", marginBottom: "1.5rem" }}>
            <a
              href="https://github.com/s-petey/validator-benchmark"
              className="button primary"
              style={{ display: "inline-flex", alignItems: "center", gap: "0.5rem", textDecoration: "none" }}
              target="_blank"
              rel="noopener noreferrer"
            >
              <svg
                aria-label="View on Github"
                style={{ width: "24px", height: "24px" }}
                fill="currentColor"
                viewBox="0 0 24 24"
              >
                <title>View on Github</title>
                <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z" />
              </svg>
              View on GitHub
            </a>
          </div>

          <p className="font-bold no-margin">https://github.com/s-petey/validator-benchmark</p>
        </div>

        <aside
          className="card"
          style={{
            borderColor: "var(--success)",
            backgroundColor: "var(--success-2)",
          }}
        >
          <h3 className="no-margin" style={{ color: "inherit" }}>
            💬 Questions & Discussion
          </h3>
          <p className="no-margin" style={{ marginTop: "0.5rem" }}>
            Have any questions, now is a great time to ask! Want to have a tour of the code more closely, great!
          </p>
        </aside>
      </div>
    </div>
  );
}

function PresentationComponent() {
  const { slide } = Route.useSearch();
  const navigate = Route.useNavigate();
  const [time, setTime] = useState(10);
  const [iterations, setIterations] = useState(2);
  const [selectedValidators, setSelectedValidators] = useState<string[]>(() => validators.map((v) => v.name));
  const [formState, setFormState] = useState({
    time,
    iterations,
    selectedValidators,
  });

  const { data, status, isPlaceholderData, progress } = useBenchmarkData(formState);

  const handleStartBenchmark = useCallback(() => {
    setFormState({
      time,
      iterations,
      selectedValidators,
    });
  }, [time, iterations, selectedValidators]);

  // Explicitly type the slides array to prevent circular inference
  const slides: SlideData[] = [
    {
      title: "Welcome",
      content: <PresentationIntroSlide />,
    },
    {
      title: "About the Speaker",
      content: <SpeakerIntroSlide />,
    },
    {
      title: "The Traditional Approach",
      content: <IntroSlide />,
    },
    {
      title: "The Evolution of API Type Safety",
      content: <OpenAPISlide />,
    },
    {
      title: "Why Runtime Validation Matters",
      content: <PerformanceMattersSlide />,
    },
    {
      title: "Property Pollution & Data Security",
      content: <PropPollutionSlide />,
    },
    {
      title: "Hey API: Pluggable Validation",
      content: <HeyAPIPluginsSlide />,
    },
    {
      title: "Standard Schema: Unifying Validation",
      content: <StandardSchemaSlide />,
    },
    {
      title: "Live Throughput Performance (Operations per Second)",
      content: (
        <ThroughputSlide
          data={data ?? []}
          onStartBenchmark={handleStartBenchmark}
          time={time}
          setTime={setTime}
          iterations={iterations}
          setIterations={setIterations}
          selectedValidators={selectedValidators}
          setSelectedValidators={setSelectedValidators}
          progress={progress}
          status={status}
          isPlaceholderData={isPlaceholderData}
        />
      ),
    },
    {
      title: "ArkType: Environment Validation Example",
      content: <ArkTypeEnvSlide />,
    },
    {
      title: "Key Performance Insights",
      content: <InsightsSlide data={data ?? []} formState={formState} />,
    },
    {
      title: "Choosing the Right Validator",
      content: <ChoosingValidatorSlide />,
    },
    {
      title: "How this benchmark was built",
      content: <ServiceWorkersSlide />,
    },
    {
      title: "Thank You & Questions",
      content: <ThankYouSlide />,
    },
  ];
  const currentSlide = Math.max(0, Math.min(slide, slides.length - 1));

  // Add keyboard navigation
  useEffect(() => {
    const handleKeyPress = (event: KeyboardEvent) => {
      if (event.key === "ArrowLeft") {
        event.preventDefault();
        const newSlide = Math.max(0, currentSlide - 1);
        if (newSlide !== currentSlide) {
          navigate({
            search: { slide: newSlide },
            replace: true,
          });
        }
      } else if (event.key === "ArrowRight") {
        event.preventDefault();
        const newSlide = Math.min(slides.length - 1, currentSlide + 1);
        if (newSlide !== currentSlide) {
          navigate({
            search: { slide: newSlide },
            replace: true,
          });
        }
      }
    };

    window.addEventListener("keydown", handleKeyPress);
    return () => window.removeEventListener("keydown", handleKeyPress);
  }, [currentSlide, slides.length, navigate]);

  const activeSlide = slides[currentSlide];

  if (!activeSlide) {
    return null;
  }

  return (
    <div className="layout-readable stack">
      <header className="header border" style={{ padding: "1rem" }}>
        <div className="split center">
          <h1 className="h1 no-margin">{activeSlide.title}</h1>
          <Link className="styled-link" to="/">← Back to Benchmarks</Link>
        </div>
      </header>

      <main className="stack">
        <div className="box glow" style={{ padding: "2rem", minHeight: "600px" }}>
          {activeSlide.content}
        </div>

        <div className="split center">
          {currentSlide === 0 ? (
            <button className="minimal" disabled style={{ opacity: 0.5 }}>
              ← Previous
            </button>
          ) : (
            <Link
              to="/presentation"
              search={{ slide: Math.max(0, currentSlide - 1) }}
              className="button primary"
              style={{ display: "inline-flex", alignItems: "center", textDecoration: "none" }}
            >
              ← Previous
            </Link>
          )}

          <div className="cluster">
            {slides.map((slide, index) => (
              <Link
                key={slide.title}
                to="/presentation"
                search={{ slide: index }}
                style={{
                  width: "12px",
                  height: "12px",
                  borderRadius: "50%",
                  display: "inline-block",
                  backgroundColor: index === currentSlide ? "var(--primary)" : "var(--gray-3)",
                  transition: "background-color 0.2s",
                }}
              />
            ))}
          </div>

          {currentSlide === slides.length - 1 ? (
            <button className="minimal" disabled style={{ opacity: 0.5 }}>
              Next →
            </button>
          ) : (
            <Link
              to="/presentation"
              search={{ slide: Math.min(slides.length - 1, currentSlide + 1) }}
              className="button primary"
              style={{ display: "inline-flex", alignItems: "center", textDecoration: "none" }}
            >
              Next →
            </Link>
          )}
        </div>

        <div className="text-center text-muted fs-xs">
          Slide {currentSlide + 1} of {slides.length}
        </div>
      </main>
    </div>
  );
}

function BulletNumber({ children }: PropsWithChildren) {
  return (
    <span
      className="circle"
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: 32,
        width: 32,
        backgroundColor: "var(--blue-9)",
        color: "white",
      }}
    >
      {children}
    </span>
  );
}
