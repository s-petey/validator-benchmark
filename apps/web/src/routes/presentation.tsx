import { validators } from "@locals/bench/benchmarks";
import type { TableResultSchema } from "@locals/bench-worker/bench.schemas";
import ComWorker from "@locals/bench-worker/comlinkWorker?worker";
import { useQuery } from "@tanstack/react-query";
import { createFileRoute, Link } from "@tanstack/react-router";
import { proxy, wrap } from "comlink";
import type { ReactElement } from "react";
import { useCallback, useEffect, useState } from "react";
import type { z } from "zod";

type TableResult = z.infer<typeof TableResultSchema>;

// Define slide type to help TypeScript inference
interface SlideData {
  title: string;
  content: ReactElement;
}

export const Route = createFileRoute("/presentation")({
  component: PresentationComponent,
  validateSearch: (search: Record<string, unknown>) => {
    return {
      slide: Number(search?.slide) || 0,
    };
  },
});

const worker = new ComWorker({
  name: "comlink-bench-worker",
});
const workerApi = wrap<import("@locals/bench-worker/comlinkWorker").ComlinkWorker>(worker);

function useBenchmarkData(formState: { time: number; iterations: number; selectedValidators: string[] }) {
  const [progress, setProgress] = useState("");

  return {
    progress,
    ...useQuery({
      queryKey: ["bench", formState.time, formState.iterations, formState.selectedValidators],
      queryFn: () =>
        workerApi
          .benchWorker(formState.time, formState.iterations, proxy(setProgress), formState.selectedValidators)
          .then((v) => {
            return v.filter((result) => formState.selectedValidators.includes(result["Task name"]));
          }),
      staleTime: Infinity,
      placeholderData: (a) => a,
    }),
  };
}

function IntroSlide() {
  return (
    <div className="text-center space-y-8">
      <h2 className="text-3xl font-semibold">The Traditional Approach</h2>
      <div className="text-xl space-y-6">
        <p>For years, we've relied on type casting API responses:</p>
        <div className="bg-gray-100 dark:bg-gray-800 p-6 rounded-lg">
          <pre className="text-left text-lg">
            <code>{`const user = await fetch('/api/user/123')
  .then(res => res.json()) as User;
// Or it can look like this
// const user = axios.get<User>("/api/user/123")


// We trust the API response matches our type
console.log(user.name); // What if this fails?
`}</code>
          </pre>
        </div>
        <p className="text-red-600 dark:text-red-400 font-semibold">
          But what happens when the API changes or returns unexpected data?
        </p>
      </div>
    </div>
  );
}

function OpenAPISlide() {
  return (
    <div className="text-center space-y-8">
      <h2 className="text-3xl font-semibold">The OpenAPI Evolution</h2>
      <div className="text-xl space-y-6">
        <p>We evolved to use OpenAPI specs with tools like Hey API:</p>
        <div className="bg-gray-100 dark:bg-gray-800 p-6 rounded-lg">
          <pre className="text-left text-base">
            <code>{`// Generated by @hey-api/openapi-ts
import { addUser } from './client/sdk.gen';
import type { AddUserData, User } from './client/types.gen';

// Type-safe at compile time
const newUser = await addUser({
  body: {
    name: 'John Doe',
    email: 'john@example.com'
  }
});

// TypeScript knows this is User type
console.log(newUser.data.name);`}</code>
          </pre>
        </div>
        <div className="bg-yellow-50 dark:bg-yellow-900/20 p-6 rounded-lg">
          <h3 className="text-lg font-semibold text-yellow-800 dark:text-yellow-300 mb-3">
            But what runtime protections do TypeScript types provide?
          </h3>
          <p className="text-base text-yellow-700 dark:text-yellow-200">
            TypeScript types are erased at runtime. Your application can still crash from unexpected data.
          </p>
        </div>
      </div>
    </div>
  );
}

function PerformanceMattersSlide() {
  return (
    <div className="space-y-8">
      <div className="text-center">
        <h2 className="text-3xl font-semibold mb-4">Why Runtime Validation Matters</h2>
        <p className="text-xl text-gray-600 dark:text-gray-300">
          When types aren't enough, validation becomes your safety net
        </p>
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
        <div className="space-y-6">
          <div className="bg-red-50 dark:bg-red-900/20 p-6 rounded-lg border-l-4 border-red-500">
            <h3 className="text-lg font-semibold text-red-800 dark:text-red-300 mb-3">🚨 The Problem</h3>
            <ul className="space-y-2 text-base text-red-700 dark:text-red-200">
              <li>• External APIs change without notice</li>
              <li>• User input can be malicious or malformed</li>
              <li>• TypeScript types are erased at runtime</li>
              <li>• Network requests can return unexpected data</li>
              <li>• Third-party integrations may fail silently</li>
            </ul>
          </div>

          <div className="bg-blue-50 dark:bg-blue-900/20 p-6 rounded-lg border-l-4 border-blue-500">
            <h3 className="text-lg font-semibold text-blue-800 dark:text-blue-300 mb-3">🛡️ The Solution</h3>
            <ul className="space-y-2 text-base text-blue-700 dark:text-blue-200">
              <li>• Validate data at runtime boundaries</li>
              <li>• Fail fast with clear error messages</li>
              <li>• Transform and sanitize untrusted input</li>
              <li>• Ensure type safety beyond compile time</li>
              <li>• Build robust, production-ready applications</li>
            </ul>
          </div>
        </div>

        <div className="space-y-6">
          <div className="bg-green-50 dark:bg-green-900/20 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-green-800 dark:text-green-300 mb-4">🎯 Critical Use Cases</h3>
            <div className="space-y-4">
              <div className="flex items-start gap-3">
                <div className="w-8 h-8 bg-green-600 text-white rounded-full flex items-center justify-center text-base font-bold">
                  1
                </div>
                <div>
                  <div className="font-medium text-green-800 dark:text-green-300">API Boundaries</div>
                  <div className="text-base text-green-700 dark:text-green-200">
                    Validate external API responses and incoming requests
                  </div>
                </div>
              </div>

              <div className="flex items-start gap-3">
                <div className="w-8 h-8 bg-green-600 text-white rounded-full flex items-center justify-center text-base font-bold">
                  2
                </div>
                <div>
                  <div className="font-medium text-green-800 dark:text-green-300">User Input</div>
                  <div className="text-base text-green-700 dark:text-green-200">
                    Forms, uploads, and any user-generated content
                  </div>
                </div>
              </div>

              <div className="flex items-start gap-3">
                <div className="w-8 h-8 bg-green-600 text-white rounded-full flex items-center justify-center text-base font-bold">
                  3
                </div>
                <div>
                  <div className="font-medium text-green-800 dark:text-green-300">Data Streams</div>
                  <div className="text-base text-green-700 dark:text-green-200">
                    Real-time events, webhooks, and message queues
                  </div>
                </div>
              </div>

              <div className="flex items-start gap-3">
                <div className="w-8 h-8 bg-green-600 text-white rounded-full flex items-center justify-center text-base font-bold">
                  4
                </div>
                <div>
                  <div className="font-medium text-green-800 dark:text-green-300">Configuration</div>
                  <div className="text-base text-green-700 dark:text-green-200">
                    Environment variables and config files
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div className="bg-gradient-to-r from-purple-50 to-pink-50 dark:from-purple-900/20 dark:to-pink-900/20 p-8 rounded-lg">
        <div className="text-center">
          <h3 className="text-2xl font-semibold text-purple-800 dark:text-purple-300 mb-4">
            ⚡ Performance is Critical
          </h3>
          <p className="text-lg text-purple-700 dark:text-purple-200 mb-6">
            Validation happens on every request, every form submission, every data transformation.
            <br />
            <strong>Choosing the right validator can make or break your application's performance.</strong>
          </p>
          <div className="grid grid-cols-1 md:grid-cols-3 gap-6 text-center">
            <div className="bg-white dark:bg-gray-800 p-4 rounded-lg shadow">
              <div className="text-2xl font-bold text-purple-600 dark:text-purple-400">10,000+</div>
              <div className="text-base text-gray-600 dark:text-gray-300">Validations per second</div>
            </div>
            <div className="bg-white dark:bg-gray-800 p-4 rounded-lg shadow">
              <div className="text-2xl font-bold text-purple-600 dark:text-purple-400">100x</div>
              <div className="text-base text-gray-600 dark:text-gray-300">Performance difference</div>
            </div>
            <div className="bg-white dark:bg-gray-800 p-4 rounded-lg shadow">
              <div className="text-2xl font-bold text-purple-600 dark:text-purple-400">DX</div>
              <div className="text-base text-gray-600 dark:text-gray-300">Developer experience matters</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}

function PropPollutionSlide() {
  return (
    <div className="space-y-8">
      <div className="text-center">
        <h2 className="text-3xl font-semibold mb-4">Unforseen issues: Property Pollution</h2>
        <p className="text-xl text-gray-600 dark:text-gray-300">External data often contains more than you expect</p>
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
        <div className="space-y-6">
          <div className="bg-red-50 dark:bg-red-900/20 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-red-800 dark:text-red-300 mb-3">⚠️ Raw API Response</h3>
            <div className="bg-gray-100 dark:bg-gray-800 p-4 rounded-lg">
              <pre className="text-base overflow-x-auto">
                <code>{`// What the API actually returns
{
  "id": 123,
  "name": "John Doe",
  "email": "john@example.com",
  // Unexpected extras:
  "internal_notes": "VIP customer",
  "password_hash": "$2b$10$...",
  "admin_flags": ["delete_users"],
  "debug_info": { "session": "abc123" },
  "experimental_features": true
}`}</code>
              </pre>
            </div>
            <p className="text-base text-red-700 dark:text-red-200 mt-3">
              This data can leak sensitive information or cause security vulnerabilities.
            </p>
          </div>

          <div className="bg-orange-50 dark:bg-orange-900/20 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-orange-800 dark:text-orange-300 mb-3">😬 Type Casting Problem</h3>
            <div className="bg-gray-100 dark:bg-gray-800 p-4 rounded-lg">
              <pre className="text-base overflow-x-auto">
                <code>{`interface User {
  id: number;
  name: string;
  email: string;
}

const user = apiResponse as User;
// All extra properties are still there!
console.log(user); 
// Shows: { id: 123, name: "John", email: "john@...", 
//          password_hash: "$2b$10$...", ... }`}</code>
              </pre>
            </div>
          </div>
        </div>

        <div className="space-y-6">
          <div className="bg-green-50 dark:bg-green-900/20 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-green-800 dark:text-green-300 mb-3">
              ✅ Zod Strips Excess Properties
            </h3>
            <div className="bg-gray-100 dark:bg-gray-800 p-4 rounded-lg">
              <pre className="text-base overflow-x-auto">
                <code>{`import { z } from 'zod';

const UserSchema = z.object({
  id: z.number(),
  name: z.string(),
  email: z.string().email()
});

const user = UserSchema.parse(apiResponse);
console.log(user);
// Shows only: { id: 123, name: "John Doe", 
//               email: "john@example.com" }

// Sensitive data automatically stripped!`}</code>
              </pre>
            </div>
            <p className="text-base text-green-700 dark:text-green-200 mt-3">
              By default, Zod removes unknown properties, protecting your application.
            </p>
          </div>

          <div className="bg-blue-50 dark:bg-blue-900/20 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-blue-800 dark:text-blue-300 mb-3">🔒 Security Benefits</h3>
            <ul className="space-y-2 text-base text-blue-700 dark:text-blue-200">
              <li>• Prevents sensitive data leaks to frontend</li>
              <li>• Reduces attack surface area</li>
              <li>• Ensures data consistency across your app</li>
              <li>• Catches unexpected API changes early</li>
              <li>• Makes debugging easier with known data shape</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  );
}

function HeyAPIPluginsSlide() {
  return (
    <div className="space-y-8">
      <div className="text-center">
        <h2 className="text-3xl font-semibold mb-4">Hey API: Pluggable Validation</h2>
        <p className="text-xl text-gray-600 dark:text-gray-300">
          Choose your validation library with OpenAPI code generation
        </p>
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
        <div className="space-y-6">
          <div className="bg-blue-50 dark:bg-blue-900/20 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-blue-800 dark:text-blue-300 mb-3">🧩 Plugin Architecture</h3>
            <p className="text-base text-blue-700 dark:text-blue-200 mb-4">
              Hey API supports validation plugins for popular libraries, giving you runtime safety with your preferred
              tool.
            </p>
            <div className="space-y-2 text-base">
              <div className="flex items-center gap-2">
                <span className="w-2 h-2 bg-green-500 rounded-full"></span>
                <strong>Zod</strong> - TypeScript-first schema validation
              </div>
              <div className="flex items-center gap-2">
                <span className="w-2 h-2 bg-blue-500 rounded-full"></span>
                <strong>Valibot</strong> - Modular, tree-shakeable
              </div>
              <div className="flex items-center gap-2">
                <span className="w-2 h-2 bg-purple-500 rounded-full"></span>
                <strong>ArkType</strong> - TypeScript-native validation
              </div>
              <div className="flex items-center gap-2">
                <span className="w-2 h-2 bg-orange-500 rounded-full"></span>
                <strong>Effect Schema</strong> - Functional approach
              </div>
            </div>
          </div>

          <div className="bg-green-50 dark:bg-green-900/20 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-green-800 dark:text-green-300 mb-3">🎯 Configuration Example</h3>
            <div className="bg-gray-100 dark:bg-gray-800 p-4 rounded-lg">
              <pre className="text-base overflow-x-auto">
                <code>{`// openapi-ts.config.ts
export default {
  input: 'openapi.json',
  output: './src/client',
  plugins: [
    '@hey-api/typescript',
    '@hey-api/sdk',
    {
      name: '@hey-api/schemas',
      type: 'zod', // or 'valibot', 'arktype'
    },
  ],
};`}</code>
              </pre>
            </div>
          </div>
        </div>

        <div className="space-y-6">
          <div className="bg-purple-50 dark:bg-purple-900/20 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-purple-800 dark:text-purple-300 mb-3">⚡ Generated Output</h3>
            <div className="bg-gray-100 dark:bg-gray-800 p-4 rounded-lg">
              <pre className="text-base overflow-x-auto">
                <code>{`// Generated schemas.gen.ts
export const UserSchema = z.object({
  id: z.number(),
  name: z.string(),
  email: z.string().email(),
});

// Generated SDK with validation
export async function getUser(id: number) {
  const response = await fetch(\`/api/users/\${id}\`);
  const data = await response.json();
  
  // Automatic runtime validation!
  return UserSchema.parse(data);
}`}</code>
              </pre>
            </div>
          </div>

          <div className="bg-yellow-50 dark:bg-yellow-900/20 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-yellow-800 dark:text-yellow-300 mb-3">✨ Benefits</h3>
            <ul className="space-y-2 text-base text-yellow-700 dark:text-yellow-200">
              <li>
                • Type-safe at compile time <strong>AND</strong> runtime
              </li>
              <li>• Consistent validation across your application</li>
              <li>• Generated from your OpenAPI spec</li>
              <li>• Choose the validator that fits your needs</li>
              <li>• Automatic schema updates with API changes</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  );
}

function StandardSchemaSlide() {
  return (
    <div className="space-y-8">
      <div className="text-center">
        <h2 className="text-3xl font-semibold mb-4">Standard Schema: Unifying Validation</h2>
        <p className="text-xl text-gray-600 dark:text-gray-300">
          A common interface for TypeScript validation libraries
        </p>
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
        <div className="space-y-6">
          <div className="bg-blue-50 dark:bg-blue-900/20 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-blue-800 dark:text-blue-300 mb-3">🎯 The Problem</h3>
            <p className="text-base text-blue-700 dark:text-blue-200 mb-4">
              Every validation library has its own API. Tools must write custom adapters for each one.
            </p>
            <div className="bg-gray-100 dark:bg-gray-800 p-4 rounded-lg">
              <pre className="text-base overflow-x-auto">
                <code>{`// Different validation APIs
const zodResult = zodSchema.parse(data);
const valibotResult = v.parse(valibotSchema, data);
const arktypeResult = arktypeSchema(data);

// Tools need adapters for each!`}</code>
              </pre>
            </div>
          </div>

          <div className="bg-green-50 dark:bg-green-900/20 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-green-800 dark:text-green-300 mb-3">⚡ The Solution</h3>
            <p className="text-base text-green-700 dark:text-green-200 mb-4">
              Standard Schema provides one interface that all validation libraries can implement.
            </p>
            <div className="bg-gray-100 dark:bg-gray-800 p-4 rounded-lg">
              <pre className="text-base overflow-x-auto">
                <code>{`// Unified validation interface
function validate<T extends StandardSchemaV1>(
  schema: T, 
  data: unknown
) {
  const result = schema['~standard'].validate(data);
  if (result.issues) throw new Error('Invalid');
  return result.value;
}`}</code>
              </pre>
            </div>
          </div>
        </div>

        <div className="space-y-6">
          <div className="bg-purple-50 dark:bg-purple-900/20 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-purple-800 dark:text-purple-300 mb-3">🏗️ Who's Behind It</h3>
            <p className="text-base text-purple-700 dark:text-purple-200 mb-3">
              Created by the makers of <strong>Zod</strong>, <strong>Valibot</strong>, and <strong>ArkType</strong>.
            </p>
            <div className="space-y-2 text-base">
              <div className="flex items-center gap-2">
                <span className="w-2 h-2 bg-green-500 rounded-full"></span>
                <span>Already implemented by 20+ validation libraries</span>
              </div>
              <div className="flex items-center gap-2">
                <span className="w-2 h-2 bg-blue-500 rounded-full"></span>
                <span>Adopted by 30+ tools and frameworks</span>
              </div>
              <div className="flex items-center gap-2">
                <span className="w-2 h-2 bg-purple-500 rounded-full"></span>
                <span>Including tRPC, TanStack, Hono, and more</span>
              </div>
            </div>
          </div>

          <div className="bg-orange-50 dark:bg-orange-900/20 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-orange-800 dark:text-orange-300 mb-3">🚀 Benefits</h3>
            <ul className="space-y-2 text-base text-orange-700 dark:text-orange-200">
              <li>
                • <strong>Ecosystem unification:</strong> Write once, support all validators
              </li>
              <li>
                • <strong>Developer choice:</strong> Pick your preferred validation library
              </li>
              <li>
                • <strong>Future-proof:</strong> New validators work automatically
              </li>
              <li>
                • <strong>Type safety:</strong> Full TypeScript support
              </li>
              <li>
                • <strong>Zero dependencies:</strong> Just a TypeScript interface
              </li>
            </ul>
          </div>

          <div className="bg-gray-50 dark:bg-gray-800 p-4 rounded-lg text-center">
            <p className="text-base font-medium">
              🌐 Learn more at{" "}
              <a
                href="https://standardschema.dev"
                className="text-blue-600 hover:underline"
                target="_blank"
                rel="noopener noreferrer"
              >
                standardschema.dev
              </a>
            </p>
          </div>
        </div>
      </div>
    </div>
  );
}

function ThroughputSlide({
  data,
  onStartBenchmark,
  time,
  setTime,
  iterations,
  setIterations,
  selectedValidators,
  setSelectedValidators,
  progress,
  status,
  isPlaceholderData,
}: {
  data: TableResult[];
  onStartBenchmark: () => void;
  time: number;
  setTime: React.Dispatch<React.SetStateAction<number>>;
  iterations: number;
  setIterations: React.Dispatch<React.SetStateAction<number>>;
  selectedValidators: string[];
  setSelectedValidators: React.Dispatch<React.SetStateAction<string[]>>;
  progress: string;
  status: string;
  isPlaceholderData: boolean;
}) {
  const throughputData = data
    .map((item) => ({
      name: item["Task name"],
      throughput: parseInt(item["Throughput avg (ops/s)"].split(" ±")[0]!.replace(/,/g, ""), 10),
      latency: parseFloat(item["Latency avg (ns)"].split(" ±")[0]!),
    }))
    .sort((a, b) => b.throughput - a.throughput);

  const maxThroughput = Math.max(...throughputData.map((d) => d.throughput));

  return (
    <div className="space-y-6">
      <div className="bg-gray-50 dark:bg-gray-800 p-4 rounded-lg">
        <h3 className="text-lg font-semibold mb-3">Benchmark Configuration</h3>

        {/* Progress section similar to index.tsx */}
        <div className="mb-4">
          <div
            className={`w-full bg-gray-200 dark:bg-gray-700 rounded-full h-2.5 ${
              status === "pending" ? "animate-pulse" : "hidden"
            }`}
          ></div>
          <div className="text-center">
            {isPlaceholderData || (status !== "success" && status !== "error") ? (
              <span className="animate-pulse">Benchmark is loading...</span>
            ) : status === "success" ? (
              <span className="text-green-500">Success!</span>
            ) : status === "error" ? (
              <span className="text-red-500">Error</span>
            ) : null}
          </div>
          {(isPlaceholderData || status === "pending") && (
            <div className="text-center">
              {progress && (
                <span className="text-gray-700 dark:text-gray-300">
                  Currently running:
                  <span className="inline-block ml-2 rounded-full px-2 py-1 text-xs font-semibold leading-5 text-white transform translate-x-1/2 bg-green-500">
                    {progress}
                  </span>
                </span>
              )}
            </div>
          )}
        </div>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-4">
          <div>
            <label className="block text-base font-medium mb-1">Time (ms)</label>
            <input
              type="number"
              min="1"
              value={time}
              onChange={(e) => setTime(Number(e.target.value))}
              className="w-full border rounded-md p-2 dark:bg-gray-700 dark:border-gray-600"
              disabled={isPlaceholderData || status === "pending"}
            />
          </div>
          <div>
            <label className="block text-base font-medium mb-1">Iterations</label>
            <input
              type="number"
              min="1"
              value={iterations}
              onChange={(e) => setIterations(Number(e.target.value))}
              className="w-full border rounded-md p-2 dark:bg-gray-700 dark:border-gray-600"
              disabled={isPlaceholderData || status === "pending"}
            />
          </div>
          <div>
            <label className="block text-base font-medium mb-1">Run Benchmark</label>
            <button
              onClick={onStartBenchmark}
              disabled={isPlaceholderData || status === "pending"}
              className="w-full px-4 py-2 bg-blue-600 text-white rounded-md disabled:opacity-50 hover:bg-blue-700"
            >
              {isPlaceholderData || status === "pending" ? "Running..." : "Start Benchmark"}
            </button>
          </div>
        </div>

        <div className="mt-4">
          <label className="block text-base font-medium mb-2">Selected Validators ({selectedValidators.length})</label>
          <div className="grid grid-cols-2 md:grid-cols-4 gap-2">
            {validators.map((v) => (
              <label key={v.name} className="flex items-center space-x-2">
                <input
                  type="checkbox"
                  checked={selectedValidators.includes(v.name)}
                  onChange={(e) => {
                    if (e.target.checked) {
                      setSelectedValidators((prev) => [...prev, v.name]);
                    } else {
                      setSelectedValidators((prev) => prev.filter((name) => name !== v.name));
                    }
                  }}
                  disabled={isPlaceholderData || status === "pending"}
                />
                <span className="text-base">{v.name}</span>
              </label>
            ))}
          </div>
        </div>
      </div>

      <div className="space-y-4">
        <div className="flex justify-between items-center">
          <div className="text-base text-gray-600 dark:text-gray-400">
            {isPlaceholderData || status === "pending" ? "Running benchmarks..." : `${data.length} validators tested`}
          </div>
          <div className="text-base text-gray-600 dark:text-gray-400">
            {throughputData.length > 0 && `${(throughputData[0]!.throughput / 1000000).toFixed(1)}M ops/s (best)`}
          </div>
        </div>

        {throughputData.length > 0 ? (
          <div className="grid gap-2">
            {throughputData.map((item, index) => (
              <div key={item.name} className="flex items-center gap-4">
                <div className="w-20 text-right font-mono text-base">{item.name}</div>
                <div className="flex-1 bg-gray-200 dark:bg-gray-700 rounded-full h-8 relative">
                  <div
                    className={`h-8 rounded-full flex items-center justify-end pr-2 text-white text-base font-semibold ${
                      index === 0
                        ? "bg-green-500"
                        : index === 1
                          ? "bg-blue-500"
                          : index === 2
                            ? "bg-purple-500"
                            : "bg-gray-500"
                    }`}
                    style={{
                      width: `${(item.throughput / maxThroughput) * 100}%`,
                    }}
                  >
                    {(item.throughput / 1000000).toFixed(1)}M ops/s
                  </div>
                </div>
              </div>
            ))}
          </div>
        ) : (
          <div className="text-center py-8 text-gray-500">
            {isPlaceholderData || status === "pending"
              ? "Running benchmarks..."
              : "Click 'Start Benchmark' to see live results"}
          </div>
        )}

        {throughputData.length > 0 && (
          <div className="text-base text-gray-600 dark:text-gray-400 mt-4">
            Higher is better. {throughputData[0]?.name} leads with{" "}
            {(throughputData[0]!.throughput / 1000000).toFixed(1)}M operations per second.
          </div>
        )}
      </div>
    </div>
  );
}

function InsightsSlide({
  data,
  formState,
}: {
  data: TableResult[];
  formState: {
    time: number;
    iterations: number;
    selectedValidators: string[];
  };
}) {
  const throughputData = data
    .map((item) => ({
      name: item["Task name"],
      throughput: parseInt(item["Throughput avg (ops/s)"].split(" ±")[0]!.replace(/,/g, ""), 10),
      latency: parseFloat(item["Latency avg (ns)"].split(" ±")[0]!),
    }))
    .sort((a, b) => b.throughput - a.throughput);

  const fastest = throughputData[0];
  const slowest = throughputData[throughputData.length - 1];
  const performanceSpread = fastest && slowest ? (fastest.throughput / slowest.throughput).toFixed(1) : "N/A";

  return (
    <div className="space-y-6">
      {/* Benchmark Configuration Summary */}
      <div className="bg-blue-50 dark:bg-blue-900/20 p-4 rounded-lg">
        <h3 className="text-lg font-semibold text-blue-800 dark:text-blue-300 mb-2">📋 Benchmark Configuration</h3>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-4 text-base">
          <div>
            <span className="font-medium">Duration:</span> {formState.time}ms
          </div>
          <div>
            <span className="font-medium">Iterations:</span> {formState.iterations}
          </div>
          <div>
            <span className="font-medium">Validators tested:</span> {formState.selectedValidators.length} of{" "}
            {validators.length}
          </div>
        </div>
        {formState.selectedValidators.length < validators.length && (
          <div className="mt-2 text-xs text-blue-700 dark:text-blue-300">
            <span className="font-medium">Selected:</span> {formState.selectedValidators.join(", ")}
          </div>
        )}
      </div>

      {throughputData.length > 0 ? (
        <>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div className="bg-green-50 dark:bg-green-900/20 p-4 rounded-lg">
              <h3 className="text-lg font-semibold text-green-800 dark:text-green-300 mb-2">🏆 Top Performers</h3>
              <ul className="space-y-1 text-base">
                {throughputData.slice(0, 3).map((item) => (
                  <li key={item.name}>
                    <strong>{item.name}:</strong> {(item.throughput / 1000000).toFixed(1)}M ops/s
                  </li>
                ))}
              </ul>
            </div>
            <div className="bg-yellow-50 dark:bg-yellow-900/20 p-4 rounded-lg">
              <h3 className="text-lg font-semibold text-yellow-800 dark:text-yellow-300 mb-2">
                ⚡ Low Latency Leaders
              </h3>
              <ul className="space-y-1 text-base">
                {throughputData
                  .sort((a, b) => a.latency - b.latency)
                  .slice(0, 3)
                  .map((item) => (
                    <li key={item.name}>
                      <strong>{item.name}:</strong> {item.latency.toFixed(0)}ns avg
                    </li>
                  ))}
              </ul>
            </div>
          </div>
          <div className="bg-red-50 dark:bg-red-900/20 p-4 rounded-lg">
            <h3 className="text-lg font-semibold text-red-800 dark:text-red-300 mb-2">📊 Performance Spread</h3>
            <p className="text-base">
              Performance varies by <strong>{performanceSpread}x</strong> between fastest ({fastest?.name}) and slowest
              ({slowest?.name}). Choose wisely based on your performance requirements.
            </p>
          </div>
        </>
      ) : (
        <div className="text-center py-8 text-gray-500">Run benchmarks to see performance insights</div>
      )}
    </div>
  );
}

function ArkTypeEnvSlide() {
  return (
    <div className="space-y-8">
      <div className="text-center">
        <h2 className="text-3xl font-semibold mb-4">ArkType: Environment Variable Validation</h2>
        <p className="text-xl text-gray-600 dark:text-gray-300">
          Type-safe environment parsing with runtime validation
        </p>
      </div>

      <div className="bg-purple-50 dark:bg-purple-900/20 p-6 rounded-lg">
        <h3 className="text-lg font-semibold text-purple-800 dark:text-purple-300 mb-3">💡 Why This Matters</h3>
        <div className="">
          <div>
            <strong className="text-purple-800 dark:text-purple-300">Fail Fast:</strong>
            <p className="text-purple-700 dark:text-purple-200">
              Catch configuration errors at startup, not in production
            </p>
          </div>
        </div>
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
        <div className="space-y-6">
          <div className="bg-gray-50 dark:bg-gray-800 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-gray-800 dark:text-gray-300 mb-3">❌ The Old Way</h3>
            <div className="bg-red-50 dark:bg-red-900/20 p-4 rounded-lg">
              <pre className="text-base overflow-x-auto">
                <code>{`// Unsafe environment access
const config = {
  port: Number(process.env.PORT) || 3000,
  database: process.env.DATABASE_URL || '',
  apiKey: process.env.API_KEY || '',
  debug: process.env.NODE_ENV === 'development'
};

// What happens when:
// - PORT is "abc"?
// - DATABASE_URL is missing?
// - API_KEY is empty?`}</code>
              </pre>
            </div>
            <p className="text-base text-red-700 dark:text-red-200 mt-3">
              Runtime errors, silent failures, and security vulnerabilities.
            </p>
          </div>

          <div className="bg-orange-50 dark:bg-orange-900/20 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-orange-800 dark:text-orange-300 mb-3">⚠️ Common Issues</h3>
            <ul className="space-y-2 text-base text-orange-700 dark:text-orange-200">
              <li>• Invalid port numbers causing server crashes</li>
              <li>• Missing database URLs in production</li>
              <li>• Empty API keys leading to authentication failures</li>
              <li>• Type coercion bugs (strings vs numbers)</li>
              <li>• No validation at startup time</li>
            </ul>
          </div>
        </div>

        <div className="space-y-6">
          <div className="bg-green-50 dark:bg-green-900/20 p-6 rounded-lg">
            <h3 className="text-lg font-semibold text-green-800 dark:text-green-300 mb-3">✅ ArkType Solution</h3>
            <div className="bg-gray-100 dark:bg-gray-800 p-4 rounded-lg">
              <pre className="text-base overflow-x-auto">
                <code>{`import { type } from 'arktype';

const envSchema = type({
  DATABASE_HOST: "string.host",
  DATABASE_PORT: "number.port",
  API_KEY: "string>10",
  "NODE_ENV?": "'development' | 'production' | 'test'",
  "DEBUG?": "string.alpha.toLowerCase.pipe(v => v === 'true')"
});

// Parse and validate at startup
const config = envSchema(process.env);

if (config instanceof type.errors) {
  console.error("Invalid environment:", config.summary);
  process.exit(1);
}

// Now 'config' is fully typed and validated!
console.log(\`Server starting on port \${config.DATABASE_PORT}\`);`}</code>
              </pre>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}

function ServiceWorkersSlide() {
  return (
    <div className="space-y-6">
      <div className="text-center">
        <h2 className="text-2xl font-semibold mb-4">Built with Web Workers</h2>
      </div>
      <div className="space-y-4">
        <div className="bg-blue-50 dark:bg-blue-900/20 p-6 rounded-lg">
          <h3 className="text-lg font-semibold text-blue-800 dark:text-blue-300 mb-3">What are Web Workers?</h3>
          <p className="text-base mb-3">
            Web Workers allow JavaScript to run computationally intensive tasks in background threads, separate from the
            main UI thread. This prevents blocking user interactions during heavy operations.
          </p>
          <p className="text-base">
            This benchmark app uses Web Workers to keep the interface smooth and responsive while running validation
            benchmarks that would otherwise freeze the UI.
          </p>
        </div>

        <div className="bg-green-50 dark:bg-green-900/20 p-6 rounded-lg">
          <h3 className="text-lg font-semibold text-green-800 dark:text-green-300 mb-3">Implementation with Comlink</h3>
          <p className="text-base mb-2">
            We use{" "}
            <a
              href="https://github.com/GoogleChromeLabs/comlink"
              className="text-blue-600 hover:underline"
              target="_blank"
              rel="noopener noreferrer"
            >
              Comlink
            </a>{" "}
            by Google Chrome Labs to simplify worker communication.
          </p>
          <p className="text-base">
            Comlink makes Web Workers feel like regular async functions, handling all the message passing complexity for
            us.
          </p>
        </div>
      </div>
    </div>
  );
}

function ChoosingValidatorSlide() {
  return (
    <div className="space-y-6">
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-4">
        <div className="bg-green-50 dark:bg-green-900/20 p-4 rounded-lg">
          <h3 className="text-lg font-semibold text-green-800 dark:text-green-300 mb-3">🚀 High Performance</h3>
          <div className="space-y-2 text-base">
            <div>
              <strong>io-ts:</strong> Functional approach, excellent for functional programming codebases
            </div>
            <div>
              <strong>ArkType:</strong> TypeScript-first, compile-time optimizations
            </div>
            <div>
              <strong>ajv:</strong> JSON Schema standard, mature ecosystem
            </div>
          </div>
        </div>
        <div className="bg-blue-50 dark:bg-blue-900/20 p-4 rounded-lg">
          <h3 className="text-lg font-semibold text-blue-800 dark:text-blue-300 mb-3">⚖️ Balanced Choice</h3>
          <div className="space-y-2 text-base">
            <div>
              <strong>zod:</strong> Great DX, TypeScript integration, good performance
            </div>
            <div>
              <strong>valibot:</strong> Modular, tree-shakeable, growing ecosystem
            </div>
          </div>
        </div>
        <div className="bg-purple-50 dark:bg-purple-900/20 p-4 rounded-lg">
          <h3 className="text-lg font-semibold text-purple-800 dark:text-purple-300 mb-3">🔧 Feature Rich</h3>
          <div className="space-y-2 text-base">
            <div>
              <strong>yup:</strong> Object schema validation, transforms, async validation
            </div>
            <div>
              <strong>joi:</strong> Rich validation rules, server-side focused
            </div>
          </div>
        </div>
      </div>
      <div className="text-center mt-8">
        <div className="bg-gray-50 dark:bg-gray-800 p-4 rounded-lg">
          <p className="text-lg font-semibold">
            Consider: Performance requirements, bundle size, ecosystem, and developer experience
          </p>
          <p className="text-base mt-2 text-gray-600 dark:text-gray-400">
            For additional insight into popularity and adoption trends, view{" "}
            <a
              href="https://tanstack.com/stats/npm?packageGroups=%5B%7B%22packages%22%3A%5B%7B%22name%22%3A%22zod%22%7D%5D%2C%22color%22%3A%22%23ef4444%22%7D%2C%7B%22packages%22%3A%5B%7B%22name%22%3A%22io-ts%22%7D%5D%2C%22color%22%3A%22%233b82f6%22%7D%2C%7B%22packages%22%3A%5B%7B%22name%22%3A%22arktype%22%7D%5D%2C%22color%22%3A%22%2310b981%22%7D%2C%7B%22packages%22%3A%5B%7B%22name%22%3A%22valibot%22%7D%5D%2C%22color%22%3A%22%23f97316%22%7D%2C%7B%22packages%22%3A%5B%7B%22name%22%3A%22yup%22%7D%5D%2C%22color%22%3A%22%2306b6d4%22%7D%2C%7B%22packages%22%3A%5B%7B%22name%22%3A%22%40sinclair%2Ftypebox%22%7D%5D%2C%22color%22%3A%22%23d946ef%22%7D%5D&range=365-days&transform=none&binType=weekly&showDataMode=all&height=400"
              className="text-blue-600 hover:underline"
              target="_blank"
              rel="noopener noreferrer"
            >
              this chart comparing install rates
            </a>{" "}
            of the different packages.
          </p>
        </div>
      </div>
    </div>
  );
}

function PresentationIntroSlide() {
  return (
    <div className="text-center space-y-8">
      <h2 className="text-4xl font-bold text-gray-800 dark:text-white mb-8">
        Type safety vs Runtime safety and why it matters
      </h2>

      <div className="space-y-6">
        <div className="bg-gradient-to-r from-blue-50 to-purple-50 dark:from-blue-900/20 dark:to-purple-900/20 p-8 rounded-lg">
          <h3 className="text-2xl font-semibold text-blue-800 dark:text-blue-300 mb-6">What We'll Cover Today</h3>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-6 text-lg">
            <div className="space-y-4">
              <div className="flex items-start gap-3">
                <div className="w-8 h-8 bg-blue-600 text-white rounded-full flex items-center justify-center text-base font-bold">
                  1
                </div>
                <div className="text-left">
                  <div className="font-medium text-blue-800 dark:text-blue-300">Why Runtime Validation Matters</div>
                  <div className="text-base text-blue-700 dark:text-blue-200">
                    The gap between TypeScript types and runtime safety
                  </div>
                </div>
              </div>

              <div className="flex items-start gap-3">
                <div className="w-8 h-8 bg-blue-600 text-white rounded-full flex items-center justify-center text-base font-bold">
                  2
                </div>
                <div className="text-left">
                  <div className="font-medium text-blue-800 dark:text-blue-300">Validation library showdown</div>
                  <div className="text-base text-blue-700 dark:text-blue-200">
                    Live benchmarks comparing popular validation libraries
                  </div>
                </div>
              </div>
            </div>

            <div className="space-y-4">
              <div className="flex items-start gap-3">
                <div className="w-8 h-8 bg-blue-600 text-white rounded-full flex items-center justify-center text-base font-bold">
                  3
                </div>
                <div className="text-left">
                  <div className="font-medium text-blue-800 dark:text-blue-300">Security Implications</div>
                  <div className="text-base text-blue-700 dark:text-blue-200">
                    Property pollution and data sanitization
                  </div>
                </div>
              </div>

              <div className="flex items-start gap-3">
                <div className="w-8 h-8 bg-blue-600 text-white rounded-full flex items-center justify-center text-base font-bold">
                  4
                </div>
                <div className="text-left">
                  <div className="font-medium text-blue-800 dark:text-blue-300">Choosing the Right Tool</div>
                  <div className="text-base text-blue-700 dark:text-blue-200">
                    Performance vs features vs developer experience
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>

        <div className="bg-green-50 dark:bg-green-900/20 p-6 rounded-lg">
          <h3 className="text-xl font-semibold text-green-800 dark:text-green-300 mb-4">🎯 Goals</h3>
          <p className="text-lg text-green-700 dark:text-green-200">
            Raise awareness of different validation librarys and show when and why to use runtime validation. Also how
            different libraries perform, and which one fits your specific use case.
          </p>
        </div>
      </div>
    </div>
  );
}

function SpeakerIntroSlide() {
  return (
    <div className="text-center space-y-8">
      <h2 className="text-3xl font-semibold mb-8">Who am I?</h2>

      <div className="flex flex-col lg:flex-row items-center justify-center gap-8">
        <div className="lg:w-1/3">
          <div className="w-48 h-48 rounded-full mx-auto overflow-hidden">
            <img
              src="https://avatars.githubusercontent.com/u/34942041?v=4"
              alt="Sam Peterson"
              className="w-full h-full object-cover"
            />
          </div>
        </div>

        <div className="lg:w-2/3 space-y-6 text-left">
          <div>
            <h3 className="text-2xl font-bold text-gray-800 dark:text-white mb-2">Sam Peterson</h3>
            <p className="text-xl text-gray-600 dark:text-gray-300">Senior Full Stack Developer</p>
          </div>

          <div className="space-y-4">
            <div className="bg-blue-50 dark:bg-blue-900/20 p-4 rounded-lg">
              <h4 className="font-semibold text-blue-800 dark:text-blue-300 mb-2">💼 Background</h4>
              <p className="text-blue-700 dark:text-blue-200">Full stack development since I started in 2018.</p>
            </div>

            <div className="bg-green-50 dark:bg-green-900/20 p-4 rounded-lg">
              <h4 className="font-semibold text-green-800 dark:text-green-300 mb-2">🚀 Interests</h4>
              <p className="text-green-700 dark:text-green-200">
                All things web, TypeScript, hacking at side projects, wood working, video games, and enjoying life with
                my wife and dog.
              </p>
            </div>

            <div className="bg-purple-50 dark:bg-purple-900/20 p-4 rounded-lg">
              <h4 className="font-semibold text-purple-800 dark:text-purple-300 mb-2">🔗 Connect</h4>
              <div className="flex gap-4 justify-between text-purple-700 dark:text-purple-200">
                <span>GitHub: @s-petey</span>
                <span>Bluesky: @sam-peterson</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}

function ThankYouSlide() {
  return (
    <div className="text-center space-y-8">
      <h2 className="text-4xl font-bold text-gray-800 dark:text-white mb-8">Thank You!</h2>

      <div className="space-y-6">
        <div className="bg-gradient-to-r from-blue-50 to-purple-50 dark:from-blue-900/20 dark:to-purple-900/20 p-8 rounded-lg">
          <h3 className="text-2xl font-semibold text-blue-800 dark:text-blue-300 mb-4">🚀 Explore the Code</h3>
          <p className="text-lg text-blue-700 dark:text-blue-200 mb-6">
            This presentation and benchmark tool are available for the public!
          </p>
          <a
            href="https://github.com/s-petey/validator-benchmark"
            className="inline-flex items-center gap-2 px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors text-lg font-semibold"
            target="_blank"
            rel="noopener noreferrer"
          >
            <svg className="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
              <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z" />
            </svg>
            View on GitHub
          </a>

          <p className="font-bold">https://github.com/s-petey/validator-benchmark</p>
        </div>

        <div className="bg-green-50 dark:bg-green-900/20 p-6 rounded-lg">
          <h3 className="text-xl font-semibold text-green-800 dark:text-green-300 mb-4">💬 Questions & Discussion</h3>
          <p className="text-green-700 dark:text-green-200 text-lg">
            Have any questions, now is a great time to ask! Want to have a tour of the code more closely, great!
          </p>
        </div>
      </div>
    </div>
  );
}

function PresentationComponent() {
  const { slide } = Route.useSearch();
  const navigate = Route.useNavigate();
  const [time, setTime] = useState(10);
  const [iterations, setIterations] = useState(2);
  const [selectedValidators, setSelectedValidators] = useState<string[]>(() => validators.map((v) => v.name));
  const [formState, setFormState] = useState({
    time,
    iterations,
    selectedValidators,
  });

  const { data, status, isPlaceholderData, progress } = useBenchmarkData(formState);

  const handleStartBenchmark = useCallback(() => {
    setFormState({
      time,
      iterations,
      selectedValidators,
    });
  }, [time, iterations, selectedValidators]);

  // Explicitly type the slides array to prevent circular inference
  const slides: SlideData[] = [
    {
      title: "Welcome",
      content: <PresentationIntroSlide />,
    },
    {
      title: "About the Speaker",
      content: <SpeakerIntroSlide />,
    },
    {
      title: "The Traditional Approach",
      content: <IntroSlide />,
    },
    {
      title: "The Evolution of API Type Safety",
      content: <OpenAPISlide />,
    },
    {
      title: "Why Runtime Validation Matters",
      content: <PerformanceMattersSlide />,
    },
    {
      title: "Property Pollution & Data Security",
      content: <PropPollutionSlide />,
    },
    {
      title: "Hey API: Pluggable Validation",
      content: <HeyAPIPluginsSlide />,
    },
    {
      title: "Standard Schema: Unifying Validation",
      content: <StandardSchemaSlide />,
    },
    {
      title: "Live Throughput Performance (Operations per Second)",
      content: (
        <ThroughputSlide
          data={data ?? []}
          onStartBenchmark={handleStartBenchmark}
          time={time}
          setTime={setTime}
          iterations={iterations}
          setIterations={setIterations}
          selectedValidators={selectedValidators}
          setSelectedValidators={setSelectedValidators}
          progress={progress}
          status={status}
          isPlaceholderData={isPlaceholderData}
        />
      ),
    },
    {
      title: "ArkType: Environment Validation Example",
      content: <ArkTypeEnvSlide />,
    },
    {
      title: "Key Performance Insights",
      content: <InsightsSlide data={data ?? []} formState={formState} />,
    },
    {
      title: "Choosing the Right Validator",
      content: <ChoosingValidatorSlide />,
    },
    {
      title: "How this benchmark was built",
      content: <ServiceWorkersSlide />,
    },
    {
      title: "Thank You & Questions",
      content: <ThankYouSlide />,
    },
  ];
  const currentSlide = Math.max(0, Math.min(slide, slides.length - 1));

  // Add keyboard navigation
  useEffect(() => {
    const handleKeyPress = (event: KeyboardEvent) => {
      if (event.key === "ArrowLeft") {
        event.preventDefault();
        const newSlide = Math.max(0, currentSlide - 1);
        if (newSlide !== currentSlide) {
          navigate({
            search: { slide: newSlide },
            replace: true,
          });
        }
      } else if (event.key === "ArrowRight") {
        event.preventDefault();
        const newSlide = Math.min(slides.length - 1, currentSlide + 1);
        if (newSlide !== currentSlide) {
          navigate({
            search: { slide: newSlide },
            replace: true,
          });
        }
      }
    };

    window.addEventListener("keydown", handleKeyPress);
    return () => window.removeEventListener("keydown", handleKeyPress);
  }, [currentSlide, slides.length, navigate]);

  const activeSlide = slides[currentSlide];

  if (!activeSlide) {
    return null;
  }

  return (
    <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 dark:from-gray-900 dark:to-gray-800">
      <header className="p-6 bg-white dark:bg-gray-900 shadow-lg">
        <div className="flex justify-between items-center">
          <h1 className="text-3xl font-bold text-gray-800 dark:text-white">{activeSlide.title}</h1>
          <Link to="/" className="text-blue-600 hover:text-blue-800 dark:text-blue-400">
            ← Back to Benchmarks
          </Link>
        </div>
      </header>

      <main className="container mx-auto px-6 py-8">
        <div className="bg-white dark:bg-gray-900 rounded-xl shadow-xl p-8 min-h-[600px]">{activeSlide.content}</div>

        <div className="flex justify-between items-center mt-8">
          {currentSlide === 0 ? (
            <div className="px-6 py-3 bg-gray-300 text-gray-500 rounded-lg cursor-not-allowed">← Previous</div>
          ) : (
            <Link
              to="/presentation"
              search={{ slide: Math.max(0, currentSlide - 1) }}
              className="px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"
            >
              ← Previous
            </Link>
          )}

          <div className="flex space-x-2">
            {slides.map((_, index) => (
              <Link
                key={index}
                to="/presentation"
                search={{ slide: index }}
                className={`w-3 h-3 rounded-full transition-colors ${
                  index === currentSlide
                    ? "bg-blue-600"
                    : "bg-gray-300 dark:bg-gray-600 hover:bg-gray-400 dark:hover:bg-gray-500"
                }`}
              />
            ))}
          </div>

          {currentSlide === slides.length - 1 ? (
            <div className="px-6 py-3 bg-gray-300 text-gray-500 rounded-lg cursor-not-allowed">Next →</div>
          ) : (
            <Link
              to="/presentation"
              search={{ slide: Math.min(slides.length - 1, currentSlide + 1) }}
              className="px-6 py-3 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors"
            >
              Next →
            </Link>
          )}
        </div>

        <div className="text-center mt-4 text-gray-600 dark:text-gray-400">
          Slide {currentSlide + 1} of {slides.length}
        </div>
      </main>
    </div>
  );
}
