{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": [],
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": ["dist/**"],
      "cache": true
    },
    "check:types": {
      "cache": true
    },
    "format": {},
    "format:fix": {}
  },
  "extends": ["//"]
}
