{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": ["dist/**"],
      "cache": true
    },
    "format": {},
    "format:fix": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
